#!/usr/bin/env bash
# Update the root bundle and the separate bundles of x-core, x-uploader, and x-objects
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle update --all
(cd x-core && bundle update --all)
(cd x-uploader && bundle update --all)
(cd x-objects && bundle update --all)
