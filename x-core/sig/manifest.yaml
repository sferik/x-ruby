# The standard libraries the signatures of x-core refer to, which rbs collection loads for code that depends on it
dependencies:
  - name: forwardable
  - name: net-http
  - name: openssl
  - name: uri
  - name: zlib
